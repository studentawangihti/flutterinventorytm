import 'package:flutter/material.dart';

class LayananScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Layanan Perbaikan")),
      body: Center(
        child: Text("Halaman Layanan (Lapor Kerusakan / Service)"),
      ),
    );
  }
}